from collections import deque
n, k = map(int,input().split())
queue = deque(range(1,n+1))
i=0

answer = []
while queue:
    x = queue.popleft()
    i+=1
    if i==:
        i=0
        answer.append(str(x))
    else:
        queue.append(x)

print("<"+", ".join(answer)+">")
